import { ErrorRequestHandler } from "../components/ErrorRequestHandler";

// page component that handles reimbursement request
export const Request = () => {
    return(
        <ErrorRequestHandler/>
    );
}